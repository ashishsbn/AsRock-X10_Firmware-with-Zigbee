--- a/minidlna.c
+++ b/minidlna.c
@@ -171,7 +171,8 @@ sighup(int sig)
 	signal(sig, sighup);
 	DPRINTF(E_WARN, L_GENERAL, "received signal %d, re-read\n", sig);
 
-	reload_ifaces(1);
+	//KKHuang: Only notify new ifaces
+	reload_ifaces(0);
 }
 
 /* record the startup time */
